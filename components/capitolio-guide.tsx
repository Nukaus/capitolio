'use client'

import { useEffect, useMemo, useState } from "react"
import { ArrowDown, ArrowLeft, ArrowRight, ArrowUp, ArrowUpRight, BedDouble, Bike, CalendarDays, Check, ChevronDown, ChevronRight, Clock3, Coffee, Compass, ExternalLink, Flag, Footprints, House, Instagram, Map, MapPin, Menu, Mountain, Navigation, PartyPopper, Route, Sailboat, Search, Sparkles, Star, TentTree, Utensils, Waves, X, Zap } from "lucide-react"
import { accommodationFeaturesNote, airbnbUrl, days, experienceData, extraAdventures, foodCategory, foods, galleryImages, heroHighlights, heroImageFallback, heroMeta, heroTitle, houseFeatures, houseStats, itinerary, legalNote, mapsUrl, menuItems, morroDoChapeu, placeholderImage, routeCities, routeNotes, stopSuggestions, tripIntro, tripTitle, wazeUrl, localStorageKey, heroScrollLabel, noAddressNote } from "@/data/trip"
import { Countdown } from "@/components/Countdown"

const navIds = ["inicio", "viagem", "trajeto", "casa", "passeios", "roteiro", "comidas", "checklist"]

function SmartImage({ src, alt, className = "", priority = false }: { src: string; alt: string; className?: string; priority?: boolean }) {
  const [current, setCurrent] = useState(src)
  return <img src={current} alt={alt} className={className} loading={priority ? "eager" : "lazy"} decoding="async" onError={() => setCurrent(src === placeholderImage ? heroImageFallback : placeholderImage)} />
}

function SectionHeading({ eyebrow, title, description, light = false }: { eyebrow: string; title: string; description?: string; light?: boolean }) {
  return <div className={`max-w-2xl ${light ? "text-white" : "text-[#163d37]"}`}><p className={`mb-3 text-xs font-semibold uppercase tracking-[0.24em] ${light ? "text-[#aed6c1]" : "text-[#66877b]"}`}>{eyebrow}</p><h2 className="font-serif text-4xl leading-tight md:text-5xl">{title}</h2>{description && <p className={`mt-4 text-base leading-7 ${light ? "text-white/70" : "text-[#60736d]"}`}>{description}</p>}</div>
}

function Navbar() {
  const [open, setOpen] = useState(false)
  const [scrolled, setScrolled] = useState(false)
  useEffect(() => { const onScroll = () => setScrolled(window.scrollY > 30); window.addEventListener("scroll", onScroll); return () => window.removeEventListener("scroll", onScroll) }, [])
  const go = (id: string) => { document.getElementById(id)?.scrollIntoView({ behavior: "smooth" }); setOpen(false) }
  return <header className={`fixed inset-x-0 top-0 z-50 transition-all ${scrolled || open ? "border-b border-[#d9e6df] bg-[#f7faf6]/95 shadow-sm backdrop-blur-lg" : "bg-gradient-to-b from-black/35 to-transparent"}`}><div className="mx-auto flex max-w-7xl items-center justify-between px-5 py-4 md:px-8"><button onClick={() => go("inicio")} className={`font-serif text-xl ${scrolled || open ? "text-[#163d37]" : "text-white"}`}>CAP<span className="text-[#b8d5b4]">.</span></button><nav className="hidden gap-5 lg:flex">{menuItems.map((item, i) => <button key={item} onClick={() => go(navIds[i])} className={`text-[11px] font-semibold uppercase tracking-wider transition hover:text-[#b8d5b4] ${scrolled ? "text-[#41645a]" : "text-white/85"}`}>{item}</button>)}</nav><button aria-label="Abrir menu" onClick={() => setOpen(!open)} className={`rounded-full p-2 lg:hidden ${scrolled || open ? "text-[#163d37]" : "text-white"}`}>{open ? <X size={22} /> : <Menu size={22} />}</button></div>{open && <nav className="border-t border-[#d9e6df] bg-[#f7faf6] px-5 pb-5 pt-2 lg:hidden">{menuItems.map((item, i) => <button key={item} onClick={() => go(navIds[i])} className="block w-full border-b border-[#e4eee9] py-3 text-left text-sm font-semibold text-[#41645a]">{item}</button>)}</nav>}</header>
}

function Hero() { return <section id="inicio" className="relative flex min-h-[680px] items-end overflow-hidden bg-[#1d514b] pb-14 pt-28 md:min-h-[780px] md:pb-20"><div className="absolute inset-0"><SmartImage src={heroImageFallback} alt="Paisagem de água e montanhas que inspira a viagem a Capitólio" className="h-full w-full object-cover" priority /><div className="absolute inset-0 bg-gradient-to-t from-[#092f2d]/95 via-[#174e4a]/35 to-[#092f2d]/20" /></div><div className="relative mx-auto w-full max-w-7xl px-5 md:px-8"><div className="max-w-2xl"><p className="mb-5 text-xs font-semibold uppercase tracking-[0.3em] text-[#b8d5b4]">Guia digital privado • Minas Gerais</p><h1 className="font-serif text-6xl leading-[.9] text-white md:text-8xl">{heroTitle}</h1><p className="mt-5 max-w-md font-serif text-2xl italic text-white/90 md:text-3xl">{tripTitle}</p><p className="mt-5 text-sm font-medium uppercase tracking-[0.18em] text-white/65">{heroMeta}</p><div className="mt-5 flex flex-wrap gap-2">{heroHighlights.map((item) => <span key={item} className="rounded-full border border-white/20 bg-white/10 px-3 py-1.5 text-xs text-white/80 backdrop-blur-sm">{item}</span>)}</div><Countdown /><button onClick={() => document.getElementById("viagem")?.scrollIntoView({ behavior: "smooth" })} className="mt-9 inline-flex items-center gap-2 rounded-full bg-[#e6bc75] px-5 py-3 text-sm font-semibold text-[#163d37] shadow-lg transition hover:-translate-y-0.5 hover:bg-[#f0cc8e]">{heroScrollLabel}<ArrowDown size={16} /></button></div><div className="mt-14 flex items-center gap-2 text-xs uppercase tracking-[0.2em] text-white/55"><span className="h-10 w-px bg-white/40" /> role para descobrir</div></div></section> }

function Overview() { return <section id="viagem" className="bg-[#f7faf6] px-5 py-20 md:px-8 md:py-28"><div className="mx-auto max-w-7xl"><SectionHeading eyebrow="A viagem" title="Pequenos planos, grandes memórias." description={tripIntro} /><div className="mt-12 grid gap-4 md:grid-cols-3">{days.map((day, i) => <div key={day.date} className="group rounded-[1.75rem] border border-[#dce9e1] bg-white p-6 shadow-sm transition hover:-translate-y-1 hover:shadow-lg"><div className={`mb-12 flex h-12 w-12 items-center justify-center rounded-2xl ${i === 0 ? "bg-[#f2e0c1] text-[#7c5c31]" : i === 1 ? "bg-[#cfe7df] text-[#23665d]" : "bg-[#dce7ce] text-[#4b6c3f]"}`}><CalendarDays size={21} /></div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-[#789187]">{day.date}</p><h3 className="mt-2 font-serif text-3xl text-[#163d37]">{day.label}</h3><div className="mt-5 h-px w-10 bg-[#bfd6c9] transition-all group-hover:w-20" /></div>)}</div></div></section> }

function RouteSection() { return <section id="trajeto" className="bg-[#edf4ee] px-5 py-20 md:px-8 md:py-28"><div className="mx-auto max-w-7xl"><SectionHeading eyebrow="Pela estrada" title="Nosso trajeto" description="A estrada também faz parte da história. Aproximadamente 280 km e cerca de 4 horas sem considerar as paradas." /><div className="mt-12 grid gap-8 lg:grid-cols-[1.15fr_.85fr] lg:items-center"><div className="rounded-[2rem] bg-[#dcebe1] p-5 md:p-8"><div className="flex flex-col gap-0 md:flex-row md:items-center md:gap-0">{routeCities.map((city, i) => <div key={city} className="flex items-center md:flex-1 md:flex-col md:gap-4"><div className="flex items-center gap-3 md:flex-col"><span className={`flex h-11 w-11 items-center justify-center rounded-full border-4 border-[#dcebe1] ${i === routeCities.length - 1 ? "bg-[#d99861]" : "bg-[#39766b]"} text-white shadow-md`}>{i === routeCities.length - 1 ? <Flag size={17} /> : <MapPin size={17} />}</span><span className="text-sm font-semibold capitalize text-[#275a50] md:text-center">{city}</span></div>{i < routeCities.length - 1 && <div className="ml-5 h-8 w-px bg-[#86ad9a] md:mx-2 md:ml-0 md:h-px md:flex-1" />}</div>)}</div><div className="mt-8 flex flex-wrap gap-2">{routeNotes.map((note) => <span key={note} className="rounded-full bg-white/70 px-3 py-1.5 text-xs font-semibold text-[#507468]">{note}</span>)}</div></div><div className="rounded-[2rem] bg-[#163d37] p-7 text-white md:p-9"><Route size={25} className="mb-8 text-[#b8d5b4]" /><p className="font-serif text-3xl">Quer navegar?</p><p className="mt-3 text-sm leading-6 text-white/65">A distância real pode variar conforme o ponto de saída em Belo Horizonte e a rota escolhida.</p><div className="mt-7 flex flex-wrap gap-3"><a href={mapsUrl} target="_blank" rel="noreferrer" className="inline-flex items-center gap-2 rounded-full bg-[#e6bc75] px-4 py-3 text-sm font-semibold text-[#163d37]">Google Maps <ExternalLink size={14} /></a><a href={wazeUrl} target="_blank" rel="noreferrer" className="inline-flex items-center gap-2 rounded-full border border-white/20 px-4 py-3 text-sm font-semibold text-white">Waze <ExternalLink size={14} /></a></div></div></div><div className="mt-14"><h3 className="font-serif text-3xl text-[#163d37]">Paradas pelo caminho</h3><p className="mt-2 text-sm text-[#6b8279]">Sugestões de parada, não um roteiro confirmado.</p><div className="mt-8 grid gap-3 md:grid-cols-5">{stopSuggestions.map((stop, i) => <div key={stop.city} className="relative rounded-2xl border border-[#d6e5db] bg-white p-4"><div className="mb-5 flex items-center justify-between"><span className="flex h-8 w-8 items-center justify-center rounded-full bg-[#e8f1e9] text-[#39766b]"><MapPin size={15} /></span><span className="text-[10px] uppercase tracking-wider text-[#8ca39a]">{stop.position}</span></div><p className="font-semibold text-[#29584f]">{stop.city}</p><p className="mt-2 text-xs leading-5 text-[#778b84]">{stop.note}</p><a href={`https://www.google.com/maps/search/?api=1&query=${encodeURIComponent(stop.city + ", MG")}`} target="_blank" rel="noreferrer" className="mt-4 inline-flex items-center gap-1 text-xs font-semibold text-[#39766b]">Navegar <ArrowUpRight size={13} /></a></div>)}</div></div></div></section> }

function Accommodation() { const [lightbox, setLightbox] = useState<number | null>(null); const images = galleryImages; const active = lightbox === null ? null : images[lightbox]; return <section id="casa" className="bg-[#f7faf6] px-5 py-20 md:px-8 md:py-28"><div className="mx-auto max-w-7xl"><div className="grid gap-10 lg:grid-cols-[1.05fr_.95fr] lg:items-center"><div><SectionHeading eyebrow="Nosso refúgio" title="Nossa casa em Capitólio" description="Um refúgio para desacelerar entre montanhas, água e bons encontros." /><div className="mt-8 grid grid-cols-2 gap-2 md:grid-cols-4">{houseStats.map((stat) => <div key={stat} className="rounded-2xl bg-[#edf4ee] p-4 text-center text-xs font-semibold text-[#477066]">{stat}</div>)}</div><div className="mt-8 grid grid-cols-2 gap-x-4 gap-y-3 text-sm text-[#5e756c]">{houseFeatures.map((feature) => <div key={feature} className="flex gap-2"><Check size={16} className="mt-0.5 shrink-0 text-[#4e8b73]" />{feature}</div>)}</div><p className="mt-8 rounded-2xl bg-[#f2e7d2] p-4 text-sm leading-6 text-[#766145]">{accommodationFeaturesNote}</p><p className="mt-3 text-xs text-[#899a93]">{noAddressNote}</p><a href={airbnbUrl} target="_blank" rel="noreferrer" className="mt-7 inline-flex items-center gap-2 rounded-full bg-[#d99861] px-5 py-3 text-sm font-semibold text-white transition hover:bg-[#c9854f]">Ver acomodação no Airbnb <ExternalLink size={15} /></a></div><div className="grid grid-cols-2 gap-3"><button onClick={() => setLightbox(0)} className="group relative col-span-2 h-64 overflow-hidden rounded-[1.75rem] bg-[#dcebe1] md:h-80"><SmartImage src={images[0].src} alt={images[0].alt} className="h-full w-full object-cover transition duration-700 group-hover:scale-105" /><span className="absolute bottom-4 left-4 rounded-full bg-white/85 px-3 py-2 text-xs font-semibold text-[#315c50]">Casa Gaivotas 95</span></button>{images.slice(1).map((image, i) => <button key={`${image.src}-${i + 1}`} onClick={() => setLightbox(i + 1)} className="group h-32 overflow-hidden rounded-[1.25rem] bg-[#dcebe1] md:h-40"><SmartImage src={image.src} alt={image.alt} className="h-full w-full object-cover transition duration-700 group-hover:scale-105" /></button>)}</div></div></div>{lightbox !== null && active && <div role="dialog" aria-modal="true" className="fixed inset-0 z-[60] flex items-center justify-center bg-[#082421]/90 p-5" onClick={() => setLightbox(null)}><div className="relative max-w-3xl" onClick={(e) => e.stopPropagation()}><SmartImage src={active.src} alt={active.alt} className="max-h-[75vh] rounded-2xl object-contain" /><button aria-label="Fechar galeria" onClick={() => setLightbox(null)} className="absolute -right-2 -top-12 rounded-full bg-white/10 p-2 text-white"><X /></button><div className="mt-4 flex items-center justify-between text-sm text-white/70"><button onClick={() => setLightbox((lightbox - 1 + images.length) % images.length)}><ArrowLeft /></button><span>{lightbox + 1} / {images.length}</span><button onClick={() => setLightbox((lightbox + 1) % images.length)}><ArrowRight /></button></div></div></div>}</section> }

function Experiences() { const [selected, setSelected] = useState<(typeof experienceData)[number] | null>(null); return <section id="passeios" className="bg-[#163d37] px-5 py-20 md:px-8 md:py-28"><div className="mx-auto max-w-7xl"><SectionHeading light eyebrow="Para viver juntos" title="Experiências que podemos viver" description="Ideias para escolher com liberdade. Os valores abaixo foram informados no planejamento e podem mudar." /><div className="mt-12 grid gap-4 md:grid-cols-2 lg:grid-cols-3">{experienceData.map((experience) => <article key={experience.id} className="group overflow-hidden rounded-[1.75rem] bg-[#23534b] transition hover:-translate-y-1 hover:bg-[#2b6259]"><div className="relative h-48 overflow-hidden bg-[#316d60]"><SmartImage src={experience.image} alt={experience.name} className="h-full w-full object-cover opacity-75 transition duration-700 group-hover:scale-105" /><div className="absolute inset-0 bg-gradient-to-t from-[#163d37] to-transparent" /><span className="absolute left-4 top-4 rounded-full bg-[#d9e8ce] px-3 py-1 text-[10px] font-bold uppercase tracking-wider text-[#39634d]">{experience.tag}</span></div><div className="p-5"><h3 className="font-serif text-2xl text-white">{experience.name}</h3><p className="mt-2 min-h-12 text-sm leading-5 text-white/65">{experience.description}</p><div className="mt-5 flex items-center justify-between text-xs text-white/55"><span>{experience.duration}</span><span>{experience.price}</span></div><button onClick={() => setSelected(experience)} className="mt-5 inline-flex items-center gap-2 text-sm font-semibold text-[#e6bc75]">Ver detalhes <ChevronRight size={16} /></button></div></article>)}</div><div className="mt-12 rounded-3xl border border-white/10 bg-white/5 p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-[#aed6c1]">Outras aventuras</p><div className="mt-4 flex flex-wrap gap-2">{extraAdventures.map((item) => <span key={item} className="rounded-full border border-white/10 px-3 py-2 text-sm text-white/70">{item}</span>)}</div></div></div>{selected && <div role="dialog" aria-modal="true" className="fixed inset-0 z-[60] flex items-end justify-center bg-[#082421]/70 p-0 md:items-center md:p-5" onClick={() => setSelected(null)}><div className="max-h-[85vh] w-full max-w-xl overflow-y-auto rounded-t-[2rem] bg-[#f7faf6] p-6 md:rounded-[2rem] md:p-8" onClick={(e) => e.stopPropagation()}><div className="flex items-start justify-between gap-4"><div><span className="text-xs font-bold uppercase tracking-wider text-[#66877b]">{selected.tag}</span><h3 className="mt-2 font-serif text-3xl text-[#163d37]">{selected.name}</h3></div><button aria-label="Fechar detalhes" onClick={() => setSelected(null)} className="rounded-full bg-[#e5eee7] p-2 text-[#41645a]"><X size={18} /></button></div><div className="mt-6 grid gap-3">{selected.details.map((detail) => <div key={detail} className="flex gap-3 rounded-xl bg-white p-4 text-sm leading-6 text-[#60736d]"><Sparkles size={16} className="mt-1 shrink-0 text-[#d99861]" />{detail}</div>)}</div><p className="mt-6 text-xs leading-5 text-[#899a93]">{legalNote}</p></div></div>}</section> }

function Itinerary() { const [active, setActive] = useState(0); return <section id="roteiro" className="bg-[#f7faf6] px-5 py-20 md:px-8 md:py-28"><div className="mx-auto max-w-7xl"><SectionHeading eyebrow="Sem pressa" title="O que fazer em cada dia" description="Um roteiro aberto para editar conforme as escolhas do grupo — sem horários inventados." /><div className="mt-10 grid gap-8 lg:grid-cols-[.38fr_1fr]"><div className="flex gap-2 overflow-x-auto lg:block">{itinerary.map((day, i) => <button key={day.date} onClick={() => setActive(i)} className={`min-w-32 rounded-2xl p-4 text-left transition lg:mb-3 lg:w-full ${active === i ? "bg-[#163d37] text-white shadow-lg" : "bg-[#eaf2eb] text-[#477066]"}`}><span className="text-xs font-bold uppercase tracking-wider opacity-70">{day.date}</span><strong className="mt-2 block font-serif text-2xl">{day.title}</strong></button>)}</div><div className="rounded-[2rem] bg-[#edf4ee] p-6 md:p-8"><p className="text-sm font-semibold text-[#477066]">{itinerary[active].date} • {itinerary[active].title}</p><div className="mt-6 grid gap-3">{itinerary[active].items.map((item, i) => <div key={item} className="flex items-center gap-4 rounded-2xl bg-white p-4"><span className="flex h-8 w-8 shrink-0 items-center justify-center rounded-full bg-[#dcebe1] text-xs font-bold text-[#39766b]">{i + 1}</span><span className="text-sm text-[#60736d]">{item}</span></div>)}</div></div></div></div></section> }

function FoodAndChecklist() { const [checked, setChecked] = useState<string[]>([]); useEffect(() => { try { const saved = window.localStorage.getItem(localStorageKey); if (saved) setChecked(JSON.parse(saved)) } catch {} }, []); const toggle = (item: string) => setChecked((current) => { const next = current.includes(item) ? current.filter((x) => x !== item) : [...current, item]; window.localStorage.setItem(localStorageKey, JSON.stringify(next)); return next }); return <><section id="comidas" className="bg-[#edf4ee] px-5 py-20 md:px-8 md:py-28"><div className="mx-auto max-w-7xl"><SectionHeading eyebrow="Sabores de Minas" title="Onde comer" description="Lugares para descobrir, sem pressa. Consulte diretamente cada estabelecimento antes de sair." /><div className="mt-10 grid grid-cols-2 gap-3 md:grid-cols-3 lg:grid-cols-5">{foods.map((food) => <div key={food} className="rounded-2xl border border-[#d7e5da] bg-white p-4"><div className="mb-8 flex h-10 w-10 items-center justify-center rounded-xl bg-[#f2e0c1] text-[#8a673b]"><Utensils size={18} /></div><p className="font-semibold text-[#315e52]">{food}</p><p className="mt-2 text-xs text-[#8a9c94]">{foodCategory}</p></div>)}</div></div></section><section id="checklist" className="bg-[#e4eee4] px-5 py-20 md:px-8 md:py-28"><div className="mx-auto max-w-7xl"><div className="grid gap-10 lg:grid-cols-[.8fr_1.2fr] lg:items-start"><SectionHeading eyebrow="Antes de sair" title="Checklist da viagem" description={`${checked.length} de ${14} itens concluídos. Marque o que já está na mala.`} /><div className="grid grid-cols-2 gap-2 md:grid-cols-3">{["Documento", "Dinheiro / cartão", "Protetor solar", "Repelente", "Roupa de banho", "Toalha", "Chinelo", "Tênis", "Roupas leves", "Carregador", "Power bank", "Medicamentos pessoais", "Óculos de sol", "Capa de chuva"].map((item) => <button key={item} onClick={() => toggle(item)} className={`flex items-center gap-3 rounded-2xl border p-4 text-left text-sm transition ${checked.includes(item) ? "border-[#8bb59a] bg-[#cfe5d4] text-[#315e52]" : "border-[#d4e2d6] bg-white text-[#60736d]"}`}><span className={`flex h-5 w-5 shrink-0 items-center justify-center rounded-md border ${checked.includes(item) ? "border-[#4e8b73] bg-[#4e8b73] text-white" : "border-[#bdd1c1]"}`}>{checked.includes(item) && <Check size={13} />}</span>{item}</button>)}</div></div><div className="mt-12 grid gap-4 md:grid-cols-3"><div className="rounded-3xl bg-[#163d37] p-6 text-white"><p className="text-xs uppercase tracking-wider text-[#aed6c1]">BH → Capitólio</p><p className="mt-2 font-serif text-4xl">≈ 280 km</p></div><div className="rounded-3xl bg-[#d99861] p-6 text-white"><p className="text-xs uppercase tracking-wider text-white/70">Ida e volta</p><p className="mt-2 font-serif text-4xl">≈ 560 km</p></div><div className="rounded-3xl bg-white p-6 text-[#60736d]"><p className="text-xs uppercase tracking-wider text-[#789187]">Quilômetros da aventura</p><p className="mt-2 font-serif text-3xl text-[#163d37]">A adicionar</p></div></div><p className="mt-6 text-xs leading-5 text-[#7b8e85]">A distância real pode variar de acordo com o ponto de saída em Belo Horizonte, deslocamentos em Capitólio e desvios/paradas.</p></div></section></> }

function MorroAndFooter() { return <><section className="bg-[#dcebe1] px-5 py-20 md:px-8 md:py-28"><div className="mx-auto grid max-w-7xl gap-8 lg:grid-cols-[1fr_.7fr] lg:items-center"><div><SectionHeading eyebrow="Um horizonte aberto" title="Morro do Chapéu" description="Um mirante para olhar Capitólio de cima e deixar a paisagem falar." /><div className="mt-8 flex flex-wrap gap-2">{morroDoChapeu.map((item) => <span key={item} className="rounded-full bg-white/70 px-4 py-2 text-sm text-[#477066]">{item}</span>)}</div></div><div className="rounded-[2rem] bg-[#39766b] p-8 text-white shadow-xl"><Mountain size={30} className="text-[#e6bc75]" /><p className="mt-8 font-serif text-3xl">Entre água, pedra e céu aberto.</p><p className="mt-4 text-sm leading-6 text-white/70">Uma parada especial para respirar fundo e guardar a vista.</p></div></div></section><footer className="bg-[#092f2d] px-5 py-12 text-white md:px-8"><div className="mx-auto flex max-w-7xl flex-col justify-between gap-8 md:flex-row md:items-end"><div><p className="font-serif text-3xl">CAPITÓLIO 2026</p><p className="mt-2 text-xs uppercase tracking-[0.2em] text-white/50">25 — 27 DE SETEMBRO</p><p className="mt-5 text-sm text-white/65">Uma viagem para guardar na memória.</p></div><button onClick={() => window.scrollTo({ top: 0, behavior: "smooth" })} className="inline-flex items-center gap-2 self-start rounded-full border border-white/20 px-4 py-2 text-sm text-white/80 transition hover:bg-white/10 md:self-auto">Voltar ao topo <ArrowUp size={15} /></button></div><p className="mx-auto mt-10 max-w-7xl border-t border-white/10 pt-5 text-xs leading-5 text-white/40">{legalNote} Tudo aqui funciona localmente no seu dispositivo.</p></footer></> }

export function CapitolioGuide() {
  const [showTop, setShowTop] = useState(false)

  useEffect(() => {
    const onScroll = () => setShowTop(window.scrollY > 560)
    window.addEventListener("scroll", onScroll, { passive: true })
    return () => window.removeEventListener("scroll", onScroll)
  }, [])

  return <div className="min-h-screen bg-[#f7faf6] text-[#163d37]"><Navbar /><main><Hero /><Overview /><RouteSection /><Accommodation /><Experiences /><Itinerary /><FoodAndChecklist /><MorroAndFooter /></main>{showTop && <button aria-label="Voltar ao topo" onClick={() => window.scrollTo({ top: 0, behavior: "smooth" })} className="fixed bottom-5 right-5 z-40 flex h-12 w-12 items-center justify-center rounded-full border border-white/60 bg-[#163d37] text-white shadow-xl transition hover:-translate-y-1 hover:bg-[#39766b] md:bottom-8 md:right-8"><ArrowUp size={18} /></button>}</div>
}

export default CapitolioGuide
